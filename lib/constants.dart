import 'package:flutter/cupertino.dart';

// hive box name
const usersBox = 'users';

const defaultPadding = EdgeInsets.fromLTRB(12.0, 8.0, 12.0, 8.0);
const buttonWidth = double.infinity;
const buttonBorderRadius = 24.0;
const defaultSpace = SizedBox(height: 15.0);
